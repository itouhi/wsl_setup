---
- name: gitのuser.nameを設定する
  community.general.git_config:
    name: user.name
    scope: global
    value: "{{ git_user_name }}"
  when: git_user_name != ""
  tags: git

- name: gitのuser.emailを設定する
  community.general.git_config:
    name: user.email
    scope: global
    value: "{{ git_user_email }}"
  when: git_user_email != ""
  tags: git

- name: 1Password SSH agentとの互換性のためcore.sshCommandをssh.exeに設定する
  community.general.git_config:
    name: core.sshCommand
    scope: global
    value: ssh.exe
  tags: git
